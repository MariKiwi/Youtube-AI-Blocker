# TODO

This file describes the recommended implementation order for YouTube AI Blocker.

The priority is:

1. Complete the server/API first
2. Build the browser client after the API is stable

## Phase 0: Project Setup

### Phase 0 decisions

- [x] Server stack: Node.js with Fastify
- [x] Database: PostgreSQL
- [x] Browser target for v1: Chromium
- [x] Browser target later: Firefox support after Chromium is stable
- [x] Voting model: always anonymous
- [x] Vote rule: one vote per user or device per video
- [x] Confidence model: simple threshold model
- [x] Unknown videos: treat as `unknown`, not `not AI`
- [x] API server setting: default to the official backend, but allow custom API URLs
- [x] Repository structure: monorepo with `/server` and `/client`
- [x] Abuse protection for v1: basic rate limiting only

### Planning and structure

- [x] Choose the server stack
- [x] Choose the database
- [x] Choose the first browser extension target
- [x] Define the repository structure for server and client code
- [x] Define a basic local development workflow

### Repository structure

- [x] Create `/server`
- [x] Create `/client`
- [x] Add a top-level documentation file for local setup
- [x] Decide whether shared types should live in `/shared` later, if both sides need the same API shapes

### Initial decisions to make

- [x] Decide how videos are identified in the database (YouTube video ID as the primary key)
- [x] Decide whether voting is anonymous, account-based, or local-only in the first version
- [x] Define the first confidence model
- [x] Decide whether the first version needs authentication or can start without login

Recommended first-version simplifications:

- [x] Use YouTube video ID as the main lookup key
- [x] Start with a simple vote model: `upvotes`, `downvotes`, `score`, `confidence_level`
- [x] Start without user accounts
- [x] Add anti-abuse protection later unless it becomes necessary immediately

### Confirmed v1 rules

- [x] No user accounts or login system
- [x] Unknown videos have no AI verdict and should not be presented as safe
- [x] One vote per device per video must be enforced as well as practical in an anonymous system
- [x] Anonymous device identity will be generated by the extension and stored locally
- [x] The extension should default to the official API server
- [x] Users can override the API server in settings
- [x] The initial "mark as AI" action also counts as the first positive vote

### Confidence model for v1

- [x] `low`: 1-4 net positive votes
- [x] `medium`: 5-14 net positive votes
- [x] `high`: 15+ net positive votes
- [x] `disputed`: close vote split or weak consensus

### Open planning items still left in Phase 0

- [x] Pick `Express` or `Fastify`
- [x] Pick a PostgreSQL access layer or ORM
- [x] Define how anonymous device identity is represented for one-vote enforcement
- [x] Define the initial local development workflow
- [x] Decide how configuration is split between development and production

### Local development workflow

- [x] Use Docker for local PostgreSQL
- [x] Run the API locally against the Dockerized database
- [x] Use environment variables for local configuration
- [x] Keep development and production configuration separated through env files and deployment environment settings

### Phase 0 outcome

Phase 0 is complete when:

- [x] The technical foundation has been chosen
- [x] The repo layout is defined as `/server` and `/client`
- [x] The anonymous voting model is defined
- [x] The confidence model is defined
- [x] The local development approach is defined

## Phase 1: Server Foundation

### Basic server setup

- [ ] Create the server project
- [ ] Add configuration support for environment variables
- [ ] Add a database connection layer
- [ ] Add migration support
- [ ] Add a basic app entrypoint
- [ ] Add health check endpoint
- [ ] Add basic error handling
- [ ] Add request logging

### Database schema

Create the first database schema for videos and votes.

- [ ] Create a `videos` table
- [ ] Store at minimum:
  - [ ] `youtube_video_id`
  - [ ] `status`
  - [ ] `upvotes`
  - [ ] `downvotes`
  - [ ] `score`
  - [ ] `confidence_level`
  - [ ] `created_at`
  - [ ] `updated_at`
- [ ] Create a `votes` table for one-vote-per-device enforcement
- [ ] Add indexes for fast lookup by YouTube video ID

Recommended first statuses:

- [ ] `flagged`
- [ ] `disputed`
- [ ] Omit records for videos with no report so they remain `unknown`

## Phase 2: API Design

Define the initial API contract before building the extension.

### Core read endpoints

- [ ] `GET /health`
- [ ] `GET /videos/:youtubeVideoId`
- [ ] `POST /videos/bulk-lookup`

The bulk lookup endpoint is important because the extension will often need to check many visible videos on a YouTube page at once.

### Core write endpoints

- [ ] `POST /videos/:youtubeVideoId/flag`
- [ ] `POST /videos/:youtubeVideoId/vote`

### Optional early endpoints

- [ ] `GET /config`
- [ ] `GET /stats`

### Response design

- [ ] Define a shared response shape for a video record
- [ ] Return vote totals and computed confidence level
- [ ] Return whether the video is currently considered AI-flagged
- [ ] Return `unknown` state when no record exists
- [ ] Return enough data for the client to render highlight severity

Example response fields:

- [ ] `youtubeVideoId`
- [ ] `isFlaggedAi`
- [ ] `upvotes`
- [ ] `downvotes`
- [ ] `score`
- [ ] `confidenceLevel`
- [ ] `status`

## Phase 3: Server Feature Implementation

### Flagging a video

- [ ] Implement logic to create a new video record when a user marks a video as AI
- [ ] Prevent duplicate initial creation issues
- [ ] Decide how repeated flag submissions should behave
- [x] Define whether the initial flag action also counts as the first positive vote

### Voting on a video

- [ ] Implement vote-for-AI logic
- [ ] Implement vote-against-AI logic
- [ ] Recalculate score and confidence after each vote
- [ ] Prevent invalid votes on unknown videos
- [ ] Enforce one vote per device per video

### Confidence calculation

Start simple and make the formula easy to inspect.

- [ ] Define how score is calculated
- [ ] Define thresholds for `low`, `medium`, and `high` confidence
- [ ] Define how a disputed result is represented
- [ ] Keep the confidence algorithm in one isolated module for future changes

Possible first version:

- [ ] Low confidence: 1-4 net positive votes
- [ ] Medium confidence: 5-14 net positive votes
- [ ] High confidence: 15+ net positive votes
- [ ] Disputed: close vote totals or weak consensus

### Validation and safety

- [ ] Validate YouTube video IDs
- [ ] Validate request payloads
- [ ] Add rate limiting for write endpoints
- [ ] Add CORS configuration for the extension
- [ ] Add input sanitization where needed

## Phase 4: Server Testing and Stabilization

### Tests

- [ ] Add unit tests for confidence calculation
- [ ] Add API tests for lookup, flagging, and voting
- [ ] Add tests for invalid requests
- [ ] Add tests for duplicate or repeated actions

### Local dev and deployment prep

- [ ] Add seed data for local development
- [ ] Add `.env.example`
- [ ] Add server run instructions
- [ ] Add deployment notes
- [ ] Define how the production database will be hosted

### Server done criteria

The server is ready for client work when:

- [ ] A client can look up one or many videos
- [ ] A client can flag a video as AI
- [ ] A client can vote for or against a flag
- [ ] Confidence levels are returned in a stable format
- [ ] The API is documented well enough for client integration

## Phase 5: Browser Extension Foundation

Only start this phase after the API contract is stable.

### Extension setup

- [ ] Create the browser extension project
- [ ] Add manifest configuration
- [ ] Set up content scripts
- [ ] Set up background/service worker logic if needed
- [ ] Set up storage for extension settings

### API integration layer

- [ ] Create a client API wrapper
- [ ] Add support for changing the API server URL in settings
- [ ] Add retry/error handling for failed requests
- [ ] Add bulk lookup support for visible videos

## Phase 6: YouTube Page Detection and Highlighting

### Detect videos on YouTube pages

- [ ] Identify which YouTube page types to support first
- [ ] Detect visible video elements on the homepage
- [ ] Detect video elements in search results
- [ ] Detect video elements in subscriptions/recommendations
- [ ] Extract YouTube video IDs reliably from page elements

### Highlight flagged videos

- [ ] Map server confidence levels to visual highlight styles
- [ ] Add visible AI labels/badges to video cards
- [ ] Add different styles for low, medium, and high confidence
- [ ] Make sure highlights remain stable when YouTube dynamically updates the page

### Blocking

- [ ] Add a setting to enable or disable blocking
- [ ] Hide or collapse videos marked as AI when blocking is enabled
- [ ] Make sure blocking does not break page layout badly
- [ ] Decide whether blocked items can be temporarily revealed by the user

## Phase 7: User Actions in the Extension

### Mark as AI

- [ ] Add a UI action to mark a video as AI
- [ ] Send the flag request to the server
- [ ] Refresh the local video state after submission

### Vote on an existing flag

- [ ] Add UI controls for voting for the AI flag
- [ ] Add UI controls for voting against the AI flag
- [ ] Update the visible confidence state after voting

### UX details

- [ ] Show loading state while requests are in progress
- [ ] Show error state if the API is unavailable
- [ ] Avoid duplicate submissions from rapid clicking

## Phase 8: Settings Page

- [ ] Create a settings page UI
- [ ] Add toggle for blocking functionality
- [ ] Add API server URL setting
- [ ] Add reset-to-default behavior
- [ ] Add future-friendly storage structure for more settings

## Phase 9: Client Testing and Hardening

- [ ] Test on major YouTube page types
- [ ] Test dynamic page updates and infinite scroll
- [ ] Test with blocking on and off
- [ ] Test behavior when the API is offline
- [ ] Test on a clean browser profile

## Suggested First Build Order

If you want the fastest path to a working prototype, build in this order:

1. Health endpoint
2. Database schema
3. Single video lookup endpoint
4. Flag video endpoint
5. Vote endpoint
6. Confidence calculation
7. Bulk lookup endpoint
8. API tests
9. Basic browser extension scaffold
10. Homepage/search page video detection
11. Highlighting flagged videos
12. Settings page with API server URL and blocking toggle
13. Mark-as-AI action
14. Vote action
15. Blocking functionality

## Recommended MVP

The first usable version should include only the essentials:

- [ ] Server with video lookup, flagging, and voting
- [ ] Simple confidence model with 3 levels
- [ ] Browser extension that highlights flagged videos
- [ ] Optional blocking toggle
- [ ] Settings page with API server URL configuration

Everything else should be treated as a later improvement.
